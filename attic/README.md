# 自造的端到端测试框架（已退役）

迁到 `pipecat eval` 之前自己写的一套。**不要用**，留着只为两个理由：

1. 里面的环境防护逻辑已经搬进 `evals/run.sh`，这里是它的出处。
2. 记录一个教训：`pipecat.evals` 早就有场景 YAML、合成用户语音、打断触发
   （`send_after: {event: llm_started, delay_ms: 500}`）、延迟预算、函数调用参数
   校验和 LLM judge，而 `pipecat/AGENTS.md` 里写着用法。没查就动手写，六百行里
   自己制造了四个缺陷：

   - **真空通过**：助手一句没说时，「没泄漏内部标注」这类判据无东西可查而全绿
   - **时间点写死秒数**：真实链路有七八秒往返，「第 4 秒打断」落在第一句还没听写
     出来之前，把两条正常行为判成了失败
   - **模型堆积**：每条场景新建 Harness 各加载一份 FunASR，六条跑下来听写从
     0.7 秒退化到 62 秒
   - **体检只查一次**：环境中途垮了照样把结果当真

   官方那套四条一条都没有。
