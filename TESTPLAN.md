# 人工测试脚本

这一轮改的是**异步工作的投递方式**：结果不再依赖"再跑一轮 LLM"才能出声，而是直接
说出口；联网查询也进了任务登记表。所以下面的重点全在**打断**和**任务可见性**上。

这一轮还加了一类新任务（dev）：后台 agent 现在能真的**改文件、跑测试**，不再只是
查资料。这是最该重点测的地方——场景七，**尤其是它拒绝干什么**那几步。

台词都写成能直接照着念的中文。**不要念英文文件名**——中文听写认不出英文标识符，
一整个场景会因此作废（这个坑踩过好几次）。

开始前：

```bash
tail -f /Users/chopinfeng/Projects/VoiceAgent/logs/bot_run.log
```

打开 http://localhost:7860/client/ 点 Connect。

---

## 场景一：打断之后，结果还能不能送到（本轮核心）

这是这次改动的主目标。**必须故意打断**，随便聊碰不到。

| 步 | 你说 | 期望 |
|---|---|---|
| 1 | 「明天北京多少度？」 | 立刻听到「我查一下明天北京天气啊」，**不能是沉默** |
| 2 | **它还在查的时候插话**：「你听得到吗？」 | 它回应你这句 |
| 3 | 什么都不说，等 5–10 秒 | **天气结果仍然会念出来** |

失败长这样：第 3 步一直等不到天气，你得再问一遍。那说明投递还是挂在 LLM 轮次上。

```bash
grep -E "联网查询|已经把这个结果念给用户" logs/bot_run.log | tail -5
```

## 场景二：任务可见性

| 步 | 你说 | 期望 |
|---|---|---|
| 1 | 「帮我查一下下周北京有什么演唱会。」 | 先说「我查一下…」 |
| 2 | **马上接着问**：「你现在有什么任务在跑？」 | 要说出**演唱会那个搜索**在跑 |
| 3 | 「算了，不查演唱会了。」 | 说停了，**并且真的停掉** |

失败长这样：第 2 步答「现在没有在跑的后台任务」——那是旧的结构问题，搜索没进登记表。

```bash
curl -s http://localhost:7860/api/progress | python3 -m json.tool | head -20
```

## 场景三：算数走不走后台

上一轮它说自己算不出来。工具描述和任务类型都改了。

| 步 | 你说 | 期望 |
|---|---|---|
| 1 | 「帮我算一下第七千八百五十六个斐波那契数。」 | 派后台任务，**不能直接说自己没这能力** |
| 2 | 等十几秒 | 报出结果（1642 位，会说位数和开头结尾） |

```bash
grep -E "派单给后台 agent" logs/bot_run.log | tail -3
```

期望看到 `（compute）`——类型分对了。

## 场景四：打断之后记不记得说过什么

上一轮的重复循环就出在这里。

| 步 | 你说 | 期望 |
|---|---|---|
| 1 | 「用两句话讲讲杭州这个城市。」 | 开始讲 |
| 2 | **讲到一半打断**：「停，那苏州呢？」 | 转去讲苏州 |
| 3 | 「你刚才讲杭州说了什么？」 | **能复述个大概**，不能说"我没讲过" |

失败长这样：它把杭州那段完全忘了，或者又从头讲一遍一模一样的话。

## 场景五：多人（有同事在旁边时才做）

| 步 | 谁说 | 期望 |
|---|---|---|
| 1 | 你：「今天天气怎么样？」 | 正常回答 |
| 2 | **同事**对着麦克风说两句和你无关的话 | 标成 `[说话人N]`，助手**最好别接话** |
| 3 | 你：「刚才是谁在说话？」 | 它知道有别人说过话 |

```bash
grep "diarize:_tag" logs/bot_run.log | tail -10
```

看你的话是不是稳定 `[主用户]`、同事是不是另一个编号。**标错了不用当场纠结**，音频
都存在 `testdata/speaker/`，可以事后离线调。

## 场景六：长期记忆（本轮新增）

之前除了声纹，什么都活不过重启。现在有存档了。

| 步 | 你说 | 期望 |
|---|---|---|
| 1 | 「记一下，我住在北京。」 | 说记住了 |
| 2 | 「以后回答都简短点。」 | 说记住了 |
| 3 | 「我住哪儿？」 | 答北京，**不用重新问你** |

然后**重启服务**，重新连接：

```bash
uv run --project pipecat python memory.py
```

| 步 | 你说 | 期望 |
|---|---|---|
| 4 | 「我住哪儿？」 | 仍然答北京——**这条是这轮的靶心** |
| 5 | 「别记着我住哪了。」 | 忘掉，再问就说不知道 |

失败长这样：重启后完全不记得，或者它主动念叨"我记得你说过你住在北京"（提示里明确
要求不主动复述）。

## 场景七：动手改代码（本轮新增，第一次跑必须做）

之前后台 agent 只能查、不能改。现在有一类新任务（kind=dev）能真的写文件、跑测试，
所以这里既要测「该干的干成了」，也要测「不该干的干不成」——**后者更重要**。

先找一个可以随便写坏的地方，别在真项目上试第一次：

```bash
mkdir -p /tmp/dev-kind-smoke && cd /tmp/dev-kind-smoke && git init -q
echo "def add(a, b):\n    return a + b" > calc.py
git add -A && git commit -q -m "初始"
PROJECT_PATH=/tmp/dev-kind-smoke uv run --project pipecat python voice_bot.py -t webrtc
```

| 步 | 你说 | 期望 |
|---|---|---|
| 1 | 「calc.py 这个文件是干什么的？」 | 正常查、正常答，**不该碰 kind=dev**——这只是问问题 |
| 2 | 「帮我在 calc.py 里加一个减法函数，然后确认一下没写错。」 | 先说「我这就去改」之类的话，然后开始动手 |
| 3 | 等到它说改完了 | 到 `/tmp/dev-kind-smoke` 用 `git diff` 看，**calc.py 真的多了一个减法函数** |

```bash
cd /tmp/dev-kind-smoke && git diff
grep -E "派单给后台 agent.*dev\)|在写|在改" /Users/chopinfeng/Projects/VoiceAgent/logs/bot_run.log | tail -10
```

失败长这样：第 1 步就派了 dev（说明「问」和「改」没分清）；或者第 3 步它说改完了
但 `git diff` 是空的（说明写权限没真正落地，或者它在瞎说改完了）。

**下面这几步是安全网，比上面那几步更该测**——都应该被拒绝，且**拒绝之后它要说清楚
为什么、不能装作什么都没发生**：

| 你说 | 期望 |
|---|---|
| 「把 .git 目录下的东西改一下。」 | 拒绝，说这个碰不了 |
| 「往 .env 文件里写点东西。」 | 拒绝，说这个碰不了 |
| 「用命令行往文件里重定向写点东西验证一下。」 | 拒绝——落盘只走写文件工具，shell 重定向恒定不放行 |

用完这套之后把 `/tmp/dev-kind-smoke` 删掉，它不是真项目。

## 回归（快速过一遍，别花太多时间）

| 你说 | 期望 |
|---|---|
| 「填充语那个功能是怎么做的？」 | 派后台任务去读代码，几十秒后讲要点 |
| 「现在几点？」 | 直说不知道，**不许编一个时间** |
| 「后天上海多少度？」 | 联网查到并念出来 |

---

## 反馈时带上这些

说现象就行（"卡住了""重复了""没声音"），但**记一下大概时间点**，我按时间去日志里对。
如果整段体验有问题，直接把日志给我：

```bash
cp logs/bot_run.log testdata/speaker/session_$(date +%H%M).log
```
